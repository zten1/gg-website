if (window.location.pathname != "/index.html"){
    window.onload = function(){
        window.scroll({
            top: 600,
            left: 0,
            behavior: 'smooth'
          });
      }
}

